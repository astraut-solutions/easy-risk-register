# Root env file template
#
# This file is used by `docker-compose.yml` for variable substitution.
# Copy it to `.env` and fill in real values as needed (do not commit `.env`).
#
# NOTE: These are only required if you run the local Supabase-compatible services
# (e.g. `docker compose --profile development up` without specifying a single service).
#
# Supabase
SUPABASE_URL=http://localhost:54321
SUPABASE_PUBLISHABLE_KEY=sb_publishable_change_me
SUPABASE_SECRET_KEY=sb_secret_change_me
# Legacy placeholders for the old keys (set only if your project still uses them):
# SUPABASE_ANON_KEY=sb_anon_change_me
# SUPABASE_SERVICE_KEY=sb_service_role_change_me
# Optional (enable local JWT verification/encryption); override with your project values.
SUPABASE_JWT_SECRET=dev-supabase-jwt-secret
ENCRYPTION_KEY=dev-encryption-key-change-me

# Postgres defaults. Update these if you customize the Supabase container setup.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=postgres
SUPABASE_ADMIN_PASSWORD=postgres

